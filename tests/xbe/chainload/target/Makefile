# Chainload test -- the target XBE.  Built standalone; its default.xbe is
# renamed and dropped into the loader's bin/ as target.xbe (see ../Makefile).
XBE_TITLE = chainload-target
NXDK_DIR ?= $(CURDIR)/../../../../third_party/nxdk
SRCS = $(CURDIR)/main.c
include $(NXDK_DIR)/Makefile
